module.exports = function (value, n) {
	"use strict";

	return {
		foo: 'bar ' + n,
		bar: 'baz ' + n,
		foo1: 'bar1 ' + n,
		bar1: 'baz1 ' + n,
		foo2: 'bar2 ' + n,
		bar2: 'baz2 ' + n,
		foo3: 'bar3 ' + n,
		bar3: 'baz3 ' + n
	};
	
};